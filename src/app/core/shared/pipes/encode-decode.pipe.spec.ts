import { EncodeDecodePipe } from './encode-decode.pipe';

describe('EncodeDecodePipe', () => {
  it('create an instance', () => {
    const pipe = new EncodeDecodePipe();
    expect(pipe).toBeTruthy();
  });
});
